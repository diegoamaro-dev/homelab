# WM-6 — Reopen & close G-F5-04 (real induced anomaly, chat + voice) — closeout

- **Date:** 2026-07-16   **Phase:** WM-6 (§9 · AD-21 · world_model_architecture.md §7)
- **Authority:** [`../04_ai_system/world_model_architecture.md`](../04_ai_system/world_model_architecture.md) §7/§9 · [`../04_ai_system/phase_f_architecture.md`](../04_ai_system/phase_f_architecture.md) (G-F5-04) · ROADMAP → Phase WM.
- **Entry state:** HEAD `b2b04670` (WM-5). **G-F5-04 OPEN** (FAIL 2026-07-01 / R-F5-A). One untracked file present (`2026-07-14_ER1_entity_resolution_finding.md`) — out of scope, untouched.
- **Status:** **G-F5-04 CLOSED — PASS on real evidence (chat + voice).** Validation only; **no code, prompt, tool, schema, loader, database or architecture change.** **STOPPED at the git gate** (not committed/pushed/tagged).

## 1. Objective & procedure
Reopen G-F5-04 and judge it on the canonical **real induced anomaly** across **chat + voice** — the R-F5-A closure (world_model_architecture.md §7; ROADMAP WM-6). Induction/restore **manual, operator, via the HA UI**; awareness refreshed by attended runs of `bin/aurora-context` (and `push-voice-context` for the voice line). Anomaly: `printer_on_overnight` (`switch.impresora_3d` on ∧ `time in overnight` [00:00–06:00 Europe/Madrid]).

## 2. Real world at validation time (reality wins)
- **`switch.impresora_3d`** — baseline off; induced on for the test; restored off. Read-after-write verified each actuation (ER-1-C1 discipline).
- **`cover.toldo`** — **genuinely OPEN (pos 100) since 2026-07-09 15:37Z**, operator-confirmed physically. This is a **real standing `awning_left_extended` anomaly**, *not* the intended closed negative control. Initial operator report ("closed") was reconciled against HA + zigbee2mqtt (`zigbee2mqtt/Toldo` reporting `OPEN`/pos 100, calibrated, linkquality 57) → **no state-integrity defect; HA is correct** (see §6 F-ASSIST-BLIND is separate).
- **`plant_water_warning`** — flapping low sensor artifact (§6 F-PLANT-FLAP); handled as a data-quality finding, not acceptance.
- **Scope decision (operator):** judge G-F5-04 on truthful surfacing of the **two stable real medium anomalies** — `printer_on_overnight` + `awning_left_extended`. Plant reported truthfully when present, absent when absent; not acceptance.

## 3. Run 1 — ABORTED (wrong endpoint exercised)
The first chat test was performed at **`https://ha.amarolab.es`** — the **Home Assistant Assist** agent — **not** the Aurora Open WebUI pipeline. It answered *"everything seems to be fine at home."* **This is not a G-F5-04 result: the F-3a awareness pipeline was never invoked.** Forensic root cause (evidence only; nothing modified):
- **Open WebUI silent** through the window — `docker logs openwebui` last line `2026-07-15T18:08:58Z`; **no `aurora_context_filter` inject line**; `webui.db` newest chat = 18:08 ("Current Date & Time"), mtime 18:08Z. The Filter injects only inside OWUI on message 1 → no OWUI request ⇒ **no injection**.
- **`GetLiveContext` = Home Assistant Assist LLM API tool** (not an Aurora component). HA Ollama conversation agent: `llm_hass_api:["assist"]`, `qwen2.5:7b-instruct`. **0 entities exposed to the conversation API** ⇒ GetLiveContext returned no entity state. Voice line `input_text.aurora_voice_context` was stale (02:15).
- **Verdict:** the Aurora World Model context (correct on disk, in-container = degraded + 3 anomalies) was never delivered; a non-Aurora, world-model-blind pathway answered → "fine." **WM-6 Run 1 = aborted, wrong endpoint. G-F5-04 kept OPEN.**

## 4. Corrected rerun (correct endpoints)
Baseline restored, printer re-induced ON (`22:49:59Z`), attended `bin/aurora-context` refresh (`22:51:52Z`) → `overall_status=degraded`, `home.anomalies=[printer_on_overnight, awning_left_extended, plant_water_warning]`.

- **CHAT (CP-A) — `https://ai.amarolab.es`, brand-new Aurora conversation — PASS (hard evidence).**
  - Injection proof: `aurora_context_filter {"event":"inject","reason":"ok:0.0h","fallback":false,"chars":414}`; `POST /api/chat/completions 200 @ 22:52:24Z`.
  - `webui.db` chat *"🏠 Degraded Home Status"* — user *"¿Está todo bien en casa ahora mismo?"*; Aurora: *"La situación actual en casa es degradada [1]. Se recomienda revisar el estado del 3D printer y retraer la awning. También, el entrance plant necesita ser regado."* citing `[source id=1]` (Home State: Degraded).
  - Surfaced **both** medium anomalies + the low advisory. Contrast Run 1 (HA Assist) *"everything fine."*
- **VOICE (CP-B) — `https://ha.amarolab.es` via AURORA v1 — PASS.**
  - Precondition: attended `push-voice-context` set `input_text.aurora_voice_context` to the current degraded line (`…3D printer on overnight; awning left extended overnight; +1 more`); read-after-write verified `@ 22:58:00Z` (contains both medium tokens).
  - AURORA v1 reported the home **degraded** and surfaced **both** medium anomalies.
  - **Evidence = operator observation + verified precondition.** The HA-Assist/Ollama pathway is INFO-silent (no independent runtime log) — expected, not suspicious (unlike Run 1, where OWUI, which logs every request, was silent).

## 5. Gate verdict & baseline restore
- **G-F5-04 — CLOSED, PASS on real evidence** (chat hard-logged; voice observed + precondition verified). Closes **R-F5-A** (awareness-consumption gap, FAIL 2026-07-01). **F-5 — CLOSED.**
- **Baseline restored `@ 23:03Z`:** `switch.impresora_3d` OFF (verified); awareness refreshed to truth — `aurora-context.json` `overall_status=degraded`, `home.anomalies=[awning_left_extended]` (printer token cleared end-to-end); `input_text.aurora_voice_context` refreshed to the awning-only line. **`cover.toldo` remains genuinely open** (real anomaly; operator decision to leave; a real weather/wear condition the token exists to catch).

## 6. Findings (recorded — NOT gate failures)
- **F-LOCALE** — Aurora (chat) embeds internal **English** anomaly tokens (*"3D printer on overnight"*, *"awning"*, *"entrance plant"*) inside Spanish prose. UX/localization improvement.
- **F-VOICE-CONTRADICT** — AURORA v1 (voice) self-contradicted in one reply: *"the system is in degraded mode"* **and** *"there are no problems currently reported."* Response-generation/UX.
- **F-PLANT-FLAP** — `sensor.sensor_planta_entrada_water_warning` flaps `none↔alarm` (~12 s `none` dips ~every 20 min; `alarm` dominant) while `sensor.sensor_planta_entrada_soil_moisture` holds **91–93 % (wet)** — contradictory / low-confidence. `plant_water_warning` presence in a snapshot depends on the sample instant; the detector is **correct** per rule `water_warning != none` (faithfully reports the live value). Data-quality / sensor-fidelity finding.
- **F-ASSIST-BLIND** — **0 entities exposed to the HA Assist conversation API** ⇒ `GetLiveContext` returns no entity state; the HA voice/Assist live-entity awareness is **blind**. AURORA v1 awareness rode the `input_text.aurora_voice_context` line (F-3b), not `GetLiveContext`. This is the **R-F5-A failure class on the voice side** (a model may trust an empty tool over the injected line) — mitigated here only because the voice line carried the anomaly. Distinct from the OWUI consumer convergence closed at WM-5; fixing it (entity exposure / tool-vs-prompt precedence) is a HA config/architecture change, **out of scope here**.
- **ER-1 reference** — deterministic entity resolution (NL→entity_id + silent no-op writes) remains independent, **OPEN/deferred** ([`2026-07-14_ER1_entity_resolution_finding.md`](2026-07-14_ER1_entity_resolution_finding.md)); not exercised by WM-6 (induction/restore were manual via the HA UI). The Run-1 HA-Assist "everything fine" is unrelated to ER-1.

## 7. Caveats
- **C1 — negative control not exercised:** the awning was a real anomaly, not the intended closed control, so "closed awning correctly absent" was not tested this run. G-F5-04 acceptance (truthful surfacing of the real degraded world) still met.
- **C2 — attended voice refresh:** the voice test required an attended `push-voice-context` (the helper was stale); a runtime data refresh mirroring the nightly 04:20 step — not a code/prompt/architecture change.
- **C3 — evidence asymmetry:** chat leg hard-logged (OWUI); voice leg = operator observation + verified precondition (HA Assist INFO-silent).

## 8. Invariant check
**No code, prompt, tool, schema, loader, database or architecture change.** Only runtime data operations: HA switch actuation (induce/restore, read-after-write verified), attended `bin/aurora-context` refreshes (regenerate the gitignored `aurora-context.{json,md,voice}`), attended `push-voice-context` (`input_text.set_value` on the voice helper). **AD-20 / INV-18 / INV-19 preserved.** HEAD unchanged (`b2b04670`).

## 9. Rollback
Nothing to roll back — no tracked file, code, prompt, tool, container, schema or DB change was made. Runtime returned to baseline (printer off; awareness reflects the real residual `awning_left_extended`). The gitignored context artifacts are regenerable; a broken artifact fail-softs the home block only.

## 10. Git gate — STOP
WM-6 change set (documentation only): **this closeout log** + triad reconciliation (`00_overview/CURRENT_STATE.md`, `ROADMAP.md`, `AMAROLAB_HANDOFF.md`) + gate-status reconciliation in `04_ai_system/phase_f_architecture.md`, `04_ai_system/world_model_architecture.md`, `04_ai_system/world_model/README.md`. **No git operation performed** (no commit / push / tag). The four findings (§6) and caveats (§7) are recorded here; F-LOCALE / F-VOICE-CONTRADICT / F-PLANT-FLAP / F-ASSIST-BLIND await their own dispositions in a later session.
