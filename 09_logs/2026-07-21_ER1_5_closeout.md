# ER-1.5 — Phase ER-1 reconciliation & closeout (2026-07-21)

- **Phase:** ER-1.5 — the final ER-1 sub-phase. **Documentation-only reconciliation
  and closeout.** No production code, schema, loader, tool, projection, or awareness
  change; no tag. Scope is **ER-1 only** — the outstanding WM-era transient drift is
  explicitly **not** in this phase (carried to a separate documentation-hygiene task,
  §6).
- **Spec:** [`../04_ai_system/entity_resolution_layer.md`](../04_ai_system/entity_resolution_layer.md) (Rev 4).
- **Closes:** the ER-1.4b transient markers left by commit `5b502c96`, the deferred
  hash-stamping (freeze Rev 4 / ER-1.3 pattern), and the ER-1 phase itself.
- **Prior:** [`2026-07-20_ER1_4b_ha_call_service_applied.md`](2026-07-20_ER1_4b_ha_call_service_applied.md).

---

## 1. Reconciliation against the frozen specification

Verified read-only against `HEAD = origin/main = 5b502c96` (branch `main`, working
tree clean). Every ER-1 deliverable in spec §10 is present and **byte-clean at HEAD**
(`git diff --quiet HEAD` over `04_ai_system/world_model`, `ai-stack/openwebui-tools`,
`ai-stack/ingest/bin/emit-entity-projection` → clean).

| Deliverable (spec §10) | State at HEAD |
|---|---|
| `world_model/_schema/entity.schema.md` | present; check 12a (`ha_entity` binding — D-ER-13) in force; `schema_version: 1` |
| `world_model/_loader/{resolution,normalize,model,validate,emit,__init__}.py` + `tests/test_resolution.py` | present; `LOADER_VERSION = "0.2.1"` |
| 6 bound entities + `environment/daylight-time.md` (aliases) | present (`printer-3d`, `awning`, `main-door`, `entrance-plant`, `zigbee-mesh`, `internet-uplink`, `daylight-time`) |
| `tools/ha_call_service.py` | **v0.2.0** (ER-1.4b) |
| `tools/ha_get_state.py` | **v0.2.1** (ER-1.4a + Rev 4 rename) |
| `lib/entity_resolver.py`, `lib/audit_helper.py`, `bin/install_tool` | present (multi-marker install; additive `extra`) |
| `ai-stack/ingest/bin/emit-entity-projection` | present (emit + `--check`) |

**Invariants confirmed against the real source (not just the docs):**

- `_evaluator/engine.py` pins `SUPPORTED_ARTIFACT_VERSIONS = (1,)`; `emit.py` writes
  `artifact_version: 1` (**D-ER-7** — additive `resolution`, no version bump).
- `schema_version: 1` unchanged (ER-1.1 additive optional field).
- The runtime artifact `world_model.generated.json` (gitignored) carries
  `artifact_version 1`, `generator.loader_version 0.2.0`, and a `resolution` block
  (`normalization` / `aliases` / `targets` / `stats`) of **33 aliases → 8 distinct
  `ha_entity` targets**. The artifact retaining `loader_version 0.2.0` while the loader
  source is `0.2.1` is **correct, not drift**: 0.2.0 generated it, ER-1 deliberately
  does not regenerate, and freshness is content-derived via `resolution_sha256`
  (`PROJECT_RULES.md` → *Content Provenance over Repository Chronology*). A version
  stamp is provenance, never freshness.

Frozen decisions **D-ER-1…14 + ER-1-C1** are unamended and reflected in the committed
behaviour (per the ER-1.1…ER-1.4b apply logs). **INV-17 holds** — D-12 remains the sole
authorization authority; the registry is consulted for observability only and never to
gate.

## 2. Gate ledger — G-ER-1…7 all accounted for (real evidence)

No gate is re-run or reopened here (`PROJECT_RULES.md` — a passed gate's closure is
history). This ledger records each closure and its authoritative evidence log.

| Gate | Condition (short) | Status | Evidence |
|---|---|---|---|
| **G-ER-1** | Loader alias validation fail-loud (check 12) | **CLOSED 2026-07-16** | `2026-07-16_ER1_2_loader_applied.md` |
| **G-ER-2** | Resolution determinism + byte-stable registry | **PASS** (loader half ER-1.2; tool ER-1.4b) | `…ER1_2…` + `2026-07-20_ER1_4b…` |
| **G-ER-3a** | Non-id miss → `unknown_entity`, zero HTTP | **PASS 2026-07-20** | `2026-07-20_ER1_4b…` |
| **G-ER-3b** | Historical unverified writes now honest | **PASS 2026-07-20** | `2026-07-20_ER1_4b…` |
| **G-ER-4** | Happy path via exact id **and** alias → `ok`+`verified` (real HA) | **PASS 2026-07-20** | `2026-07-20_ER1_4b…` |
| **G-ER-5** | No WM/awareness regression | **CLOSED 2026-07-17** | `2026-07-17_ER1_2_G-ER-5_operational_closeout.md` |
| **G-ER-6** | Projection failure rehearsal (producer + consumer) | **CLOSED** — producer (ER-1.3); consumer read (ER-1.4a) + write (ER-1.4b) | `…ER1_3…` + `…ER1_4a…` + `…ER1_4b…` |
| **G-ER-7** | Backward compatibility (read + write halves) | **PASS** — read (ER-1.4a) + write (ER-1.4b) | `…ER1_4a…` + `…ER1_4b…` |

All seven evidence logs verified present in `09_logs/`. **G-ER-3b** is the defect fix:
the 13 historical unverified writes across 7 non-existent ids now return
`applied_unverified`, never a false `ok`.

**Test-suite corroboration.** The gate closures rest on the dated real-data evidence
(42→43 loader + 36 evaluator green, recorded at ER-1.2/1.3/1.4b). A fresh local re-run
was **intentionally not performed at ER-1.5**: no test harness is provisioned in this
environment, and standing up one is outside a documentation-only closeout — re-running a
passed gate is not how a gate is honoured. The committed test files are present and
byte-clean at HEAD.

## 3. Deferred hash-stamping — closed

The freeze Rev 4 / C1-protocol records deferred the tool content hash "to the next
reconciliation, per the ER-1.3 pattern." Captured here as durable provenance
(sha256 of the committed tool sources at HEAD):

| Tool | Version | sha256 (git source, HEAD) |
|---|---|---|
| `ha_get_state.py` | 0.2.1 | `b35570c458249da4c5df39c4439f07e5543d08d4c6de106cc9ca0633ee9a5a40` |
| `ha_call_service.py` | 0.2.0 | `aea2bec3b9932b18ccdf63eab9b709010cf6bdd9a944521c8a1d3519b4f865e1` |

Baseline for rollback comparison (from the ER-1.4b log): the pre-cutover installed
v0.1.0 `ha_call_service` row was sha256
`3ba8808e8729d878fd0a4e1d65f54bd76ee0dfb3e9bed2fe75e5ce095c526ab9`.

## 4. Rollback & observability review

**Rollback (spec §7; ER-1.4b §5) — confirmed complete and recoverable.** Revert order is
tools → projection → loader → `aliases`. Both v0.1.0 tool baselines are recoverable from
git (`ha_call_service` at `5b502c96^`, `ha_get_state` at `3ad8779f^` — both
`version: 0.1.0`); tools revert as single `webui.db` rows (D-WM5-5 pattern), the
projection is fully regenerable, loader/`aliases` revert by `git revert`. No DB
migration, container, cron, awareness, or `artifact_version` change is involved.
**Honest caveat preserved:** reverting the tools restores the known-bad defect
(v0.1.0's unverified success claim).

**Observability — confirmed live in the committed tools.** The audit line carries the
additive `registry_target` (D-ER-14), `verified`, and `state_after`; pre-existing keys
stay byte-identical (audit_helper `extra` merges after the fixed keys). The result-code
taxonomy (spec §4.1) is in force — `applied_unverified`, `unknown_entity`, and
`resolver_unavailable` are all present in `ha_call_service.py`. Projection staleness is
reported by the host-side `emit-entity-projection --check` (content-derived; the sole
freshness authority), and is never surfaced at the tool boundary (spec §9).

## 5. Documentation reconciliation (triad)

Per *Transient Operational Status* (the rule ER-1 itself originated), commit `5b502c96`
published "at the git gate" for ER-1.4b into all three triad documents; that status went
false the instant it was pushed. Cleared to the durable fact **`committed + pushed
5b502c96`**, and Phase ER-1 marked **COMPLETE**:

- `CURRENT_STATE.md` — header phase paragraph, Next-milestone, Last-completed,
  Last-updated, and pending-item 8.
- `ROADMAP.md` — Last-updated header, ER-1.4b row, Rev 4 row (hash note → this log),
  ER-1.5 row.
- `AMAROLAB_HANDOFF.md` — Last-updated, ER-1 Next block.

**Not touched** (deliberately, this phase): the WM-era "STOPPED at the git gate" markers
(WM-4 `476e0ae8` / WM-5 `b2b04670`) and the *Documentation Hygiene — Follow-up* section
that tracks them; the *process-rule* uses ("STOP at the git gate"); the Secrets policy
("not committed in plain text"); and every historical `09_logs/` entry (exempt evidence —
*Historical Documentation*).

## 6. Carried forward — outside ER-1

Recorded so nothing is silently dropped; none blocks ER-1 closure.

1. **F-ER13-1** — `bin/aurora-context` `write_atomic` derives its temp path via
   `path.with_suffix(".tmp")`, so `aurora-context.json` and `.md` share one temp path,
   and it holds no run-lock. Harmless today (sequential single-process writes); the
   hazard is a manual run overlapping the 04:15 cron. Pre-existing; ER-1 explicitly does
   **not** change `bin/aurora-context` (spec §10). Needs a durable backlog home.
   Record: `2026-07-17_ER1_3_projection_applied.md` §4.
2. **Projection regeneration scheduling** — deferred by operator decision (spec §9); the
   loader and emitter run by hand, so a stale projection degrades **alias resolution
   only** (direct ids unaffected — D-ER-9). Accepted residual risk.
3. **WM-era triad transient drift** — pre-existing, already tracked in `ROADMAP.md` →
   *Documentation Hygiene — Follow-up*; a **separate documentation-hygiene task** after
   ER-1 is formally closed (operator-scoped, this phase).

## 7. Closure

**Phase ER-1 — Deterministic Entity Resolution — COMPLETE.** ER-1.0 → ER-1.5 all
delivered; the design (Rev 4) is unamended; D1 (resolution absent) and D2 (unverified
success) are both fixed; G-ER-1…7 are closed/passed on real evidence; rollback and
observability are documented and confirmed; the triad carries no false ER-1 transient
status. Aurora's read path resolves natural language, and the write path verifies before
it claims success — the 13 historical unverified writes can no longer be reported as
successful.

## 8. Git gate

**At the Git Approval Gate — STOP.** No `git commit` / `push` / `tag` without explicit
operator approval requested immediately beforehand (`PROJECT_RULES.md` → *Operator Git
Approval*). No tag this phase (operator instruction).
